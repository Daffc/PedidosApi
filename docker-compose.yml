services:
  dotnet:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
      - sqlite_data:/workspace/persistence
    tty: true
    command: sleep infinity

volumes:
  sqlite_data: